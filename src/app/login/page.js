/* src/app/login/page.js | Login route — hosts AuthDialog (login intent) | Sree | 2026-06-27 */

import { AuthScreen } from "@/components/marketing/AuthScreen";

export default function LoginPage() {
  return <AuthScreen intent="login" />;
}
/* - - - - - - - - - - - - - - - - */
